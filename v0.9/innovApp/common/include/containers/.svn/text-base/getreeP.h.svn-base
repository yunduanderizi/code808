#ifndef GETREEP_H
#define GETREEP_H

// binary tree routines that allow trees of objects (nodes) to be created that
// have a UNIQUE 32 bit key.  A tree of order log2(n) deep is required to
// store n randomly distributed objects.  The tree is not a balanced tree and
// therfore will perform badly if the objects encountered contain ordered
// sequences.
// GEBTREE.C is a generalised version of the same code that is used for keys
// of any size.

#include <containers/getree.h>

#ifdef __cplusplus
extern "C" {
#endif

#ifdef __cplusplus
}
#endif

#endif /* GETREEP_H */
