#ifndef DITRAFFICICONP_H
#define DITRAFFICICONP_H

#include <display/ditrafficicon.h>

struct TViewport;

void IconDisplayRank_DefineTraffic(TIconDisplayRank *pThis,
                                   UINT8 importance,
                                   const TPos *mapPos,
                                   struct TViewport *viewport,
                                   TTrafficEventId eventId,
                                   TAppTrafficEventClass eventClass,
                                   BOOL8 diversionRecommended,
                                   BOOL8 avoided,
                                   UINT8 iconIndex);

#endif
